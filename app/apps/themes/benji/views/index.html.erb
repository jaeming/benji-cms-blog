<% @portfolio = PostType.find_by(slug: "portfolio") %>
<% @post = PostType.find_by(slug: "post") %>
<h2>Portfolio</h2>
<% @portfolio.posts.take(3).each do |portfolio| %>
  <h3> <%= portfolio.title %> </h3>
  <p>
    <%=raw portfolio.content %>
  </p>
<% end %>
<hr>
<h2>Posts</h2>
<% @post.posts.take(3).each do |post| %>
  <h3> <%= post.title %> </h3>
  <p>
    <%=raw post.content %>
  </p>
<% end %>
